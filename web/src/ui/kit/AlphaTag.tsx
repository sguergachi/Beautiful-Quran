/** Product-stage mark. Same letterspaced whisper as a settings section
 *  label — never a chip, fill, or stroke. */
export function AlphaTag() {
  return (
    <span className="alpha-tag" aria-label="Alpha, still in development">
      Alpha
    </span>
  )
}
